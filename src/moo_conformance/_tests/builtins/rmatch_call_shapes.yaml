name: rmatch_call_shapes
description: Table-driven TYPE_ANY coverage for rmatch().

requires:
  builtins:
    - rmatch

tests:
  - name: rmatch_third_arg_{kind}
    permission: wizard
    table:
      columns: [kind, expr]
      rows:
        - [float, "1.5"]
        - [obj, "#0"]
        - [str, '"x"']
        - [err, "E_ARGS"]
        - [list, "{}"]
        - [map, "[1 -> 2]"]
    code: 'rmatch("foo", "foo", {expr})'
    expect:
      type: list
